/// <reference path="../typings/main.d.ts"/>

import changesetjs from '../lib/index';
import {expect} from 'chai';

describe('changesetjs', () => {
  it('should work', async() => {
    expect(true).to.equal(true);
  });
});

